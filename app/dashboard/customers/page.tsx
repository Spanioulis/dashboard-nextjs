import styles from '../../ui/home.module.css';

export default function CustomersPage() {
    return (
        <div className="p-5 bg-gray-50">
            <h1 className={`${styles.btn} ${styles.border}`}>Customers Page</h1>
        </div>
    );
}
